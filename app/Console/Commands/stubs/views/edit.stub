@extends('layouts.app')

@section('content')
    <h1>Edit {{ lowerModelName }}</h1>

    @include('{{ pluralLowerModelName }}.route-menu')

    @include('{{ pluralLowerModelName }}.form', [
        'action' => route('{{ pluralLowerModelName }}.update', ${{ lowerModelName }}),
        '_method' => 'PUT',
    ])
@endsection
